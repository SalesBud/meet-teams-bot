export enum MeetingBotStatus {
    PENDING = 'pending',
    RECORDING = 'recording',
    PROCESSING = 'processing',
    COMPLETE = 'complete',
    FAILED = 'failed'
}

export const minWordsLength = 10;
